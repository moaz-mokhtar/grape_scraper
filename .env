# General
LOCALHOST=127.0.0.1

# Server env vars
SERVER_PORT=8000
SERVER_HOST=${LOCALHOST}:${SERVER_PORT}

# Datbase env vars
DATABASE_URL=postgres://user:password@localhost:5432/db

# Scrape URL
SOURCE_DOMAIN=https://catalog.data.gov
SOURCE_URL=${SOURCE_DOMAIN}/dataset
# to scrape page 1 url is: https://catalog.data.gov/dataset/?page=1